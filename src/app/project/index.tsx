import React from 'react';

const Project = ({ location }) => (
    <div>
        <h1>My counter</h1>
        <div>
            {'test'}
            {console.log({ location })}
        </div>
    </div>
);

export default Project;
